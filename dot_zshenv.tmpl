# .zshenv — 全シェル/スクリプトに効く環境変数。
#
# 実体は ~/.config/shell/env.sh。bash(Claude Code が使う)と共有するため POSIX sh で
# 書いてある。ここには zsh 固有のものだけを置く。
# 役割分担は docs/zsh-cheatsheet.md の「シェルの役割分担」。

# ignore /etc/zprofile, /etc/zshrc, /etc/zlogin, and /etc/zlogout
# ref. http://zsh.sourceforge.net/Doc/Release/Files.html
# ref. http://zsh.sourceforge.net/Doc/Release/Options.html#index-GLOBALRCS
unsetopt GLOBAL_RCS

# copied from /etc/zprofile
# system-wide environment settings for zsh(1)
# env.sh より先に走らせる。ここで入るシステムのパスの前に、env.sh が自前のパスを足す。
if [ -x /usr/libexec/path_helper ]; then
  eval `/usr/libexec/path_helper -s`
fi

# -U: keep only the first occurrence of each duplicated value
# ref. http://zsh.sourceforge.net/Doc/Release/Shell-Builtin-Commands.html#index-typeset
# env.sh 側でも重複は弾いているが、ここより後(rc.d や外部)で足された分にも効かせる。
typeset -U PATH path MANPATH manpath

_shell_env="${XDG_CONFIG_HOME:-$HOME/.config}/shell/env.sh"
[ -r "$_shell_env" ] && source "$_shell_env"
unset _shell_env

export PATH MANPATH HOMEBREW_PATH
